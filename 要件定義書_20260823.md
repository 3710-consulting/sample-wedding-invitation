# 結婚式 Web招待状テンプレート化 要件定義書

- 作成日: 2026-08-23
- 更新日: 2026-08-23
- 位置づけ: 副業商材化（[結婚式_招待状](../結婚式_招待状/)で制作した自分の結婚式サイトをベースに、複数クライアントへ制作・納品できるテンプレートに一般化する）
- ステータス: ドラフト（v0.1）。3章・8章・12章の各方針は未確定

## 1. 概要

自分の結婚式のために制作したWeb招待状サイト（[要件定義書_20260802.md](../結婚式_招待状/要件定義書_20260802.md)、[DESIGN.md](../結婚式_招待状/DESIGN.md)）をベースに、他のカップルの結婚式にもカスタマイズして提供できる「テンプレート」として一般化する。提供形態は**代行制作モデル**（クライアントごとに自分がテンプレートをカスタマイズして納品する）とし、セルフサーブ型（クライアントが自身で入力し自動生成する形）は採用しない。

## 2. 目的・背景

- 自分の結婚式サイト制作で得たノウハウ・実装資産（3Dフォトギャラリー、GAS連携の出欠フォーム、クラシック系デザインシステム）を1回限りで終わらせず、副業の商材として再利用したい。
- 案件ごとにゼロから作ると工数がかさむため、**設定ファイル・デザイントークンの差し替えで大部分を賄える構成**にし、1案件あたりの制作コスト（工数）を下げる。
- 既存実装は自分の結婚式向けに固有の内容（配色・フォント・コンテンツ）が入っているため、他カップルに転用する前提でどこまで固定しどこまで可変にするかを明確にする必要がある。

## 3. 想定顧客・提供形態（要検討）

- 想定顧客: 結婚式を控えたカップル（紙の招待状に代わる／補完するWeb招待状を希望する層）。集客導線・単価・想定案件数は未定（要検討）。
- 提供形態: **代行制作モデル**。ヒアリング→設定ファイル・デザイントークンのカスタマイズ→GAS/スプレッドシート・ホスティングのセットアップ→納品、という流れをクライアントごとに自分が実施する。
- 案件フォルダー運用: 受注後は既存の運用ルールに倣い `案件/<クライアント名>/` にサブフォルダーを作成し、本テンプレートをコピー・カスタマイズして進める。本フォルダー（`結婚式_招待状テンプレート/`）自体は雛形・ドキュメントの保管場所とし、案件固有の個人情報は置かない。

## 4. テンプレートに含む機能（単一プラン）

プラン分けは行わず、既存サイトの機能一式を標準搭載の単一プランとして提供する。

| # | 機能 | 概要 | 固定/可変 |
|---|------|------|-----------|
| F1 | カウントダウン | 挙式日時までの残り日・時・分・秒をリアルタイム表示 | 日時はクライアントごとに可変 |
| F2 | 出欠確認フォーム | 新郎側／新婦側の選択、氏名、フリガナ、アレルギー・食事制限、お連れ様（人数上限なし・動的追加） | 項目構成は基本固定、文言は可変 |
| F3 | フォーム→スプレッドシート自動反映 | GAS Web App経由でクライアントごとのスプレッドシートに自動追記 | クライアントごとに個別のGAS・スプレッドシートを新規発行 |
| F4 | 写真ギャラリー（立体表現） | Three.js等によるWebGL空間演出。レイアウト調整可能 | 写真・枚数・配置は可変、演出ロジックは共通 |
| F5 | 会場情報 | 会場名・住所・地図（Googleマップ埋め込み）・アクセス・駐車場 | 内容は全面的に可変 |
| F6 | 挨拶文・新郎新婦プロフィール | 挨拶文、プロフィール、馴れ初めエピソード | 内容は全面的に可変 |
| F7 | 公式LINEアカウント誘導 | 「友だち追加」ボタン／QRコードでLINE公式アカウントへ誘導 | URL・QRはクライアントごとに差し替え |
| F8 | レスポンシブ対応 | スマートフォン前提、PCでも崩れないレイアウト | 共通（変更不可） |

## 5. カスタマイズ可能範囲

既存実装（[実装/frontend](../結婚式_招待状/実装/frontend), [実装/frontend-3d](../結婚式_招待状/実装/frontend-3d), [実装/gas](../結婚式_招待状/実装/gas)）を一般化するにあたり、案件ごとに差し替える範囲を明確にする。

| 区分 | 内容 | 差し替え方法 |
|------|------|--------------|
| コンテンツ | 挙式日時、挨拶文、プロフィール、会場情報、写真、LINE URL | 設定ファイル（`content.js`相当）を案件ごとに用意 |
| デザイントークン | アクセントカラー、（必要に応じて）配色比率、フォント | [DESIGN.md](../結婚式_招待状/DESIGN.md)のカラーパレット・タイポグラフィをトークン化し、既定値（クラシック／モノトーン＋差し色）から差し替え可能にする |
| データ連携先 | Googleスプレッドシート、GAS Web App URL | 案件ごとに新規のスプレッドシート・GASプロジェクトを発行 |
| ホスティング先・ドメイン | Vercel/Netlifyのプロジェクト、独自ドメイン有無 | 案件ごとに新規プロジェクトを作成 |
| レイアウト構成（画面順序等） | 6章の画面構成の順序・有無 | 基本は固定。個別要望があれば都度対応（テンプレートの標準機能ではない） |

## 6. 画面構成（テンプレート標準・1ページスクロール）

既存サイトと同一構成を踏襲する。

1. ヒーローセクション（挙式日・カウントダウン）
2. 挨拶文
3. 新郎新婦プロフィール
4. フォトギャラリー（立体演出）
5. 会場情報（地図・アクセス）
6. 出欠確認フォーム
7. 公式LINE誘導
8. フッター（問い合わせ先など）

## 7. データ項目定義（Googleスプレッドシート）

既存サイトの設計（[要件定義書_20260802.md](../結婚式_招待状/要件定義書_20260802.md) 6章）をそのままテンプレート標準とする。案件ごとに新規スプレッドシートを発行し、列構成（タイムスタンプ／新郎側・新婦側／出欠／氏名・フリガナ（代表者）／アレルギー／お連れ様人数／お連れ様情報／メッセージ・備考）は変更しない前提とする。

## 8. 技術構成（要検討）

- **ベース実装**: 既存実装（[実装/frontend](../結婚式_招待状/実装/frontend), [実装/frontend-3d](../結婚式_招待状/実装/frontend-3d), [実装/gas](../結婚式_招待状/実装/gas)）を流用・一般化する。フロントエンド2種（Three.js非依存版／frontend-3d版）のうちどちらをテンプレートの正式なベースにするかは要検討（両方残す場合はメンテコストが増える点に留意）。
- **コンテンツ管理**: 案件ごとの差し込み内容は1つの設定ファイルに集約する既存方針を踏襲し、個人情報・案件固有情報がテンプレート本体（Git管理する共通コード）に混在しないよう分離する。
- **リポジトリ戦略**: テンプレート本体を1つの基点（プライベートリポジトリ／ローカルの雛形）として管理し、受注のたびに複製して案件フォルダーへ展開する運用を想定。テンプレート側の改善を各案件へ後追いで反映する手段（差分マージ等）は運用が固まってから検討する。
- **バックエンド／データ保存**: 案件ごとにGoogle Apps Script（`doPost`）＋Googleスプレッドシートを新規発行する既存方式を踏襲。
- **ホスティング**: Vercel または Netlify（無料枠）。案件ごとに新規プロジェクトを作成。
- **地図**: Googleマップ埋め込み（iframe）。

## 9. 制作フロー（案件ごとの標準プロセス・案）

1. ヒアリング（挙式日時、挨拶文、プロフィール、会場情報、写真、LINE URL、アクセントカラー等の希望）
2. `案件/<クライアント名>/` フォルダーを作成し、本テンプレートを複製
3. 設定ファイル・デザイントークンをヒアリング内容に基づいて差し替え
4. クライアント用のGoogleスプレッドシート・GAS Web Appを新規発行し接続
5. Vercel/Netlifyで新規プロジェクトを作成し公開（必要に応じ独自ドメイン設定）
6. クライアントに確認依頼（実機・スマートフォンでの表示確認、フォーム送信テスト）
7. 納品（URL・QRコード・管理用スプレッドシートの共有）
8. 保守対応の範囲を事前合意（軽微な文言修正のみ／有償追加対応 等、9章末尾で要検討）

保守・追加対応の範囲（無償/有償の線引き）は未確定（要検討）。

## 10. 非機能要件

既存サイトの非機能要件（[要件定義書_20260802.md](../結婚式_招待状/要件定義書_20260802.md) 8章）に加え、複数クライアント運用を前提とした項目を追加する。

- **モバイルファースト**: 既存方針を踏襲。
- **パフォーマンス**: Three.js演出のフォールバック方針を既存踏襲。低スペック端末・高齢層利用への配慮は全案件共通。
- **アクセシビリティ**: 既存方針を踏襲。
- **プライバシー・案件間の分離**: 各クライアントのサイトは`noindex`設定とし、URLは招待状経由でのみ配布する既存方針を踏襲。加えて、**案件ごとに個人情報・写真等の機密データを別フォルダー・別スプレッドシート・別GASプロジェクトに完全に分離**し、テンプレート本体（共通コード）には特定クライアントの個人情報を一切含めない。
- **公開範囲**: パスワード等は設けず、URLを知っていれば誰でもアクセス可能とする既存方針を踏襲（案件ごとに変更の要望があれば個別対応）。

## 11. コスト試算（概算・テンプレート運用時）

- クライアントごとのランニングコスト: 既存試算（ホスティング無料枠、GAS/スプレッドシート無料、独自ドメインのみ年間数百〜数千円）を踏襲。
- 自分側の制作コスト（工数）: テンプレート化により1案件あたりの実装工数を圧縮することが目的。具体的な想定工数・単価は未定（要検討）。

## 12. 未確定事項・要検討（残課題）

- 想定顧客層・集客導線（3章）
- 価格設定・契約形態（単価、着手金の有無、支払いタイミング等）
- テンプレートのベースとするフロントエンド実装（Three.js非依存版／frontend-3d版のどちらを正式版とするか、8章）
- 保守・追加対応の無償／有償の線引き（9章）
- 想定案件数・年間キャパシティ
